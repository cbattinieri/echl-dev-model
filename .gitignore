# Python
__pycache__/
*.py[cod]

# Secrets — the EP key is ONLY ever an env var, never a file
.env

# Local scratch / notebooks / spreadsheets
.ipynb_checkpoints/
*.ipynb
*.xlsx
*.log

# OS
.DS_Store
Thumbs.db

# NOTE: model/*.pkl, data/feeder_snapshot.csv, and docs/data/players.json ARE
# tracked — they are the committed model + keyless data snapshot + app payload.
